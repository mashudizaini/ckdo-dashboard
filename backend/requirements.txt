setuptools>=69.0.0
wheel

# Web Framework
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Database
sqlalchemy==2.0.27
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
sqlparse==0.5.1

# Oracle
oracledb==2.1.2

# Auth / Keycloak
python-keycloak==3.9.1
python-jose[cryptography]==3.3.0
cryptography==43.0.3
httpx>=0.28.0,<1

# Cache / Queue
redis==5.0.1
celery==5.3.6

# Validation
pydantic>=2.7.0,<3
pydantic-settings>=2.3.0,<3
email-validator==2.1.0

# AI
anthropic==0.40.0
# Was pinned at 1.24.4 (May 2024) — ~2 years and 4 minor versions behind;
# bumped to the current release for accumulated MuPDF core bug fixes
# (better PDF parsing/text-extraction robustness feeds directly into the
# OCR-fallback paths in chatbot.py, hr_emagazine.py, and the Document
# Converter). Checked the 1.25-1.28 release notes for the specific APIs
# this repo calls (get_text, get_textpage_ocr, Document.open/save) — no
# breaking changes found, only bug fixes and additive new args.
PyMuPDF==1.28.0
#openai-whisper==20231117
faster-whisper==1.0.3

# RAG Chatbot — embeddings (local Ollama, bge-m3) + pgvector
pgvector==0.3.6

# Document Converter — PDF/DOCX/image -> RAG-friendly markdown (OCR + table
# structure recognition via RapidOCR/onnxruntime, CPU-only, no torch needed)
docling==2.117.0

# Data Processing
pandas==2.2.0
openpyxl==3.1.5
xlrd==2.0.1
python-calamine==0.7.0
python-docx>=1.2.0,<2
python-pptx==1.0.2

# HR API
# talenta-python==0.1.0

# File Upload
python-multipart==0.0.9
aiofiles==23.2.1

# Utils
python-dotenv==1.0.1
structlog==24.1.0
tenacity==8.2.3

# SSH
paramiko==3.4.0

# Coretax browser automation
playwright>=1.44.0

# Oracle EBS Backup Recovery (ported from the standalone ebs-backup-dashboard
# app) — schedule polling, MinIO object storage client, cron-expression math
apscheduler==3.10.4
minio==7.2.9
croniter==3.0.3
