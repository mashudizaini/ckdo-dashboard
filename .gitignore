.env
*.pyc
__pycache__/
.venv/
node_modules/
dist/
*.egg-info/
uploads/
exports/
results/
*.log
postgres_data/
redis_data/
# Oracle Client - too large for GitHub, must be installed on server
oracle_client/*.so*
oracle_client/*.so
oracle_client/*.dll
oracle_client/*.dylib
oracle_client/oci.so*

# Claude Code memory (assistant-specific, not project code)
.claude/

# Real employee data samples — contains PII, never commit
sumber/

# Runtime-generated, not source
backend/downloads/
nginx/ssl/

# Google service account keys — deployed directly to the server (same as
# .env), never through git.
backend/credentials/

# Runtime content managed by the app (uploaded via HR e-Magazine admin) — not source
e-magazine/magazines/*
!e-magazine/magazines/.gitkeep
backend/app/data/daily_sales.json

# Server-local backups made during deploys/migrations
.env.bak*
*.bak-*
e-magazine.pre-migration-*/
